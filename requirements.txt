# local-mcp MCP server dependencies (deployment / core runtime)
# NOTE: crawl4ai is intentionally excluded here. It pulls in playwright +
# a large ML stack (~600 MB) that exceeds Vercel's bundle limit and cannot
# run a headless browser on serverless anyway. Install it locally for the
# browser-render fallback with: pip install "local-mcp[browser]"
# NOTE: the multi-agent team engine (run_agent_team) is also an optional extra
# — it pulls in Microsoft AutoGen (autogen-agentchat + autogen-ext[ollama]).
# Install it with: pip install "local-mcp[agents-autogen]"
mcp>=1.2.0
httpx>=0.27.0
beautifulsoup4>=4.12
lxml>=5.0
markdownify>=0.13
python-dotenv>=1.0
Pillow>=10.0
pytesseract>=0.3.13
pypdf>=5.0
python-docx>=1.1
python-pptx>=1.0
requests>=2.31.0
